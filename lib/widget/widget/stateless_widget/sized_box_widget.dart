import 'package:flutter/material.dart';

class SizedBoxWithH extends StatelessWidget {
  const SizedBoxWithH(this.height, {super.key});

  final double? height;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: height,
    );
  }
}

class SizedBoxW extends StatelessWidget {
  const SizedBoxW(this.width, {super.key});

  final double? width;

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: width,
    );
  }
}

class SizedBox0H extends StatelessWidget {
  const SizedBox0H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 0,
    );
  }
}

class SizedBox5H extends StatelessWidget {
  const SizedBox5H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 5,
    );
  }
}

class SizedBox10H extends StatelessWidget {
  const SizedBox10H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}

class SizedBox15H extends StatelessWidget {
  const SizedBox15H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}

class SizedBox20H extends StatelessWidget {
  const SizedBox20H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}

class SizedBox25H extends StatelessWidget {
  const SizedBox25H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 25,
    );
  }
}

class SizedBox50H extends StatelessWidget {
  const SizedBox50H({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 50,
    );
  }
}

class SizedBox0W extends StatelessWidget {
  const SizedBox0W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 0,
    );
  }
}

class SizedBox5W extends StatelessWidget {
  const SizedBox5W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 5,
    );
  }
}

class SizedBox10W extends StatelessWidget {
  const SizedBox10W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}

class SizedBox15W extends StatelessWidget {
  const SizedBox15W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}

class SizedBox20W extends StatelessWidget {
  const SizedBox20W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20,
    );
  }
}

class SizedBox25W extends StatelessWidget {
  const SizedBox25W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 25,
    );
  }
}

class SizedBox50W extends StatelessWidget {
  const SizedBox50W({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 50,
    );
  }
}
