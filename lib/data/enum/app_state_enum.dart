enum AppThemeStateEnum { dark, light }

enum AppFirstOpenAppStateEnum { firstOpen, opened }

enum AppAuthStateEnum { loading, unAuthorized, authorized }

enum AppStateEnum { successful, fail, doing }
